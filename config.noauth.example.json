{
  "_comment": "Example: No Authentication (Open Relay - Use with Caution)",
  "_comment_description": "Server accepts mail from any client without authentication. Only use in trusted networks!",
  "_comment_warning": "WARNING: Running an open SMTP relay on the public internet will likely result in abuse",
  
  "server": {
    "port": 2525,
    "_comment_port": "Non-standard port for testing or internal use",
    
    "hostname": "internal-smtp.local",
    "security_mode": "none",
    "_comment_security_mode": "No encryption - suitable for internal networks only",
    
    "max_email_size": 10485760,
    "max_concurrent_connections": 50,
    "read_timeout": 60,
    "write_timeout": 60
  },
  
  "auth": {
    "enabled": false,
    "_comment": "Authentication disabled - any client can send mail",
    "credentials": []
  },
  
  "routes": [
    {
      "pattern": "alerts@internal.local",
      "webhook": {
        "url": "http://monitoring.internal.local:8080/alerts",
        "timeout": 30,
        "_comment": "No secret - suitable for internal trusted network"
      }
    },
    {
      "pattern": "*@internal.local",
      "webhook": {
        "url": "http://mail-processor.internal.local:8080/process",
        "timeout": 30
      }
    }
  ],
  
  "defaults": {
    "webhook": {
      "url": "http://mail-processor.internal.local:8080/default",
      "timeout": 30
    }
  }
}
